<%# $(".item-button-container__left").html("#{escape_javascript(render partial: 'like', locals: { item: @item, items: @items, likes: @likes, like: @like})}") %>

$("#like-<%= @item.id %>").html("<%= j(render partial: 'likes/like', locals: { item: @item, likes: @likes, like: @like}) %>");